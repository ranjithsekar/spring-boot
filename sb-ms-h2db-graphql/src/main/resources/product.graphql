schema {
  query: ProductQuery
}

type ProductQuery {
  allProducts: [Product]
  product(id: String): Product
}

type Product{
  id: String
  name: String
  category: String
  price: String
}